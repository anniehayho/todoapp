import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      width: '100%',
      padding: 15,
      marginVertical: 5,
      alignItems: 'center',
    },
    logo: {
      width: 70,
      height: 70,
    },
  });

  export default styles